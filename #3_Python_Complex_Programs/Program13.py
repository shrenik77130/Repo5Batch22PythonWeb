#WAP to input base and height of traingle. Calculate and
#show its Area

base = float(input("Enter Base of Traingle :"))
height = float(input("Enter Height of Traingle :"))

area = 0.5*base*height

print(f"Area of Triangle = {area}")
